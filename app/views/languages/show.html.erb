<h1><%= @language.name %></h1>

<% @snippets.each do |s| %>
	<h2><%= link_to s.title, snippet_path(s) %></h2>
	Category: <%= s.language.name %>
	<%= markdown("~~~~~#{s.language.name.downcase}\n"+s.body+"\n~~~~~") %>
	<hr>
<% end %>
